{
  "name": "eve-lexware-office-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" run build",
    "test": "pnpm -r --filter \"./packages/*\" run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "pnpm@10.33.0"
}
